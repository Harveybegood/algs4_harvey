package test;

import edu.princeton.cs.algs4.StdOut;

public class rowLength {
    public static void main(String[] args) {
        double[][] a = {
                {1.1, 1.2, 1.2, 1.3},
                {1.3, 3.1},
                {1.4, 4.1}
        };
        StdOut.println("Row, a.length: " + a.length + " \n" + "Col, a[0].length: " + a[0].length);
    }
}
