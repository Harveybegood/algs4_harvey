package Chapter1_5_Union_Find;
/*
*   In the weighted quick-union algorithm, suppose that we set id[find(p)] to q instead of to id[find(q)].
*   Would the resulting algorithm be correct?
*
* */
public class Ex1_5_10 {
    // Yes, but it would increase the tree height, so the performance guarantee would be invalid.
}
